#include <stdio.h>

int main()
{
    int i,num;
    //i=0
    num = 0;
    // printf("Enter a number\n");
    // scanf("%d",&num);
    for (i = 0; i < 25; i++)
    {

        num = num + 1;
        printf("%d\n", num+25);
    }

    // {

    /*      index = index + 1;
          printf("%d\n", index);

      } while (index < num);*/
      return 0;
}
